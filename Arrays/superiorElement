vector<int> superiorElements(vector<int>&a) {
    // Write your code here.
    vector<int> ans;
    int i=a.size()-1;
    ans.push_back(a[i]);
    int max=a[i];
    for(int j=i-1;j>=0;j--){
        if(a[j]>max){
            ans.push_back(a[j]);
            max=a[j];
        }
    }
    sort(ans.begin(),ans.end());
    
    return ans;
}
