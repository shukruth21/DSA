vector<int> runningSum(vector<int>& nums) {
        vector<int> ans(nums.size(),0);
        int pre=0;
        for(int i=0;i<nums.size();i++){
            ans[i]=pre+nums[i];
            pre=ans[i];
        }
        return ans;
    }
