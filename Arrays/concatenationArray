vector<int> getConcatenation(vector<int>& nums) {
        vector<int> ans;
        int n=2*nums.size();
       for(int j=0;j<n;j++){
           if(j<nums.size()){
               ans.push_back(nums[j]);
           }
           else{
               int i=j-nums.size();
               ans.push_back(nums[i]);
           }
       }
       return ans;
    }
