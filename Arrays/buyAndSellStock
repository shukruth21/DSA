class Solution {
public:
    int maxProfit(vector<int>& prices) {
        int m=100000,pro=0;
        for(int i=0;i<prices.size();i++){
             if(prices[i]<m){
                 m=prices[i];
             }
             pro=max(pro,(prices[i]-m));
        }
        return pro;
    }
};
