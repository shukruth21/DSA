
class Solution {
public:
    int maxSubArray(vector<int>& nums) {
        int m=-100000,curr=0;
        for(auto i:nums){
            curr += i;
            m=max(m,curr);
            if(curr<0){
                curr=0;
            }
        }
        return m;

    }
};
